const PlaceholderPage = ({ title }) => {
  return (
    <div>
      <h2>{title}</h2>
      <p>
        This module is scaffolded for Phase 1. In Phase 2, full CRUD flows, validations, filters,
        and server integration will be implemented.
      </p>
    </div>
  );
};

export default PlaceholderPage;
